# Production Environment Configuration for DigitalOcean App Platform
# This file serves as a template - actual values will be set via App Platform environment variables

# Flask Configuration
FLASK_ENV=production
FLASK_APP=app_socketio.py
PORT=5000

# Database Configuration (will be injected by DigitalOcean)
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Security Configuration (MUST be changed in production)
SECRET_KEY=change-this-secret-key-in-production
JWT_SECRET_KEY=change-this-jwt-secret-key-in-production
JWT_ACCESS_TOKEN_EXPIRES=3600

# CORS Configuration
CORS_ORIGINS=https://your-frontend-domain.com

# Upload Configuration
UPLOAD_FOLDER=/tmp/uploads
MAX_CONTENT_LENGTH=16777216

# Redis Configuration (optional - for session storage and caching)
# REDIS_URL=redis://localhost:6379/0

# Email Configuration (optional - for notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Logging Configuration
LOG_LEVEL=INFO

# WebSocket Configuration  
SOCKETIO_ASYNC_MODE=eventlet
SOCKETIO_CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com